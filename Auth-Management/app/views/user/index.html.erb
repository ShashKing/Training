<h1>Home Page</h1>
     <%if current_user%>
     
     <div>
     	<%= current_user.name%></div><br>
   <%= image_tag (current_user.image), style:"height:100px;width:200px;"%><br>
   <%=current_user.email%><br/><br>
  
     <%end%>
    <% unless current_user %>
    	<button class="button"> <%= link_to "Sign in with Facebook", user_facebook_omniauth_authorize_path %> </button>
       
    <% else %>
      <button class="button">  <%= link_to "Logout", destroy_user_session_path, method: :delete %></button>
    <% end %>