
<%= link_to 'Previous', paging_students_path(search_str: params[:search_str], page: params[:page].to_i-1, total_pages: @students.total_pages), remote: true %> | 
<%= link_to 'Next', paging_students_path(search_str: params[:search_str], page: params[:page].to_i+1, total_pages: @students.total_pages), remote: true %>
