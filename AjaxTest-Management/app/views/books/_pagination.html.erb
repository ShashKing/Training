<%#= link_to 'Previous', paging_books_path(search_str: params[:search_str], page: params[:page].to_i-1, total_pages: @books.total_pages), remote: true %> | 
<%#= link_to 'Next', paging_books_path(search_str: params[:search_str], page: params[:page].to_i+1, total_pages: @books.total_pages), remote: true %>
